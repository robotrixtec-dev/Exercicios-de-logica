"Exercicio: Maior e diferente"


TextWindow.WriteLine("xxxxx") "Peça o numero ao usuario"
TextWindow.ReadLine(num1)
num_1 = ("Faça com que o usuario coloque o valor do numero")
TextWindow.WriteLine("xxxxx") "Peça o numero ao usuario"
TextWindow.ReadLine(num1)
num_2 = ("Faça com que o usuario coloque o valor do numero")

"crie as variaveis que comportam o menor valor, o maior valor e a diferença entre eles"

sub Maior
    if () Then
        "Crie a lógica que define qual entre os dois numeros digitado pelo usuario é o maior"
    EndIf
endSub

sub Diferenca
        "Crie a lógica que define qual é a diferença numeros digitado pelo usuario é o maior"
endSub


"Problemas para resolver:
    1. Crie a lógica que compare os dois números e determine qual é o maior.
    2. Crie a lógica que calcule a diferença entre os dois números.
    3. Explique por que o resultado atual está incorreto.
    4. Corrija o código para que ele funcione corretamente e exiba o maior número e a diferença entre eles.
"