Text.Window.Write("Digite o valor A: ")
valorA = TextWindow ReadNumber()

Textwindow.Write("Digite o valor B: ")
valorB = Text.Window Readnumber()

total = valorA + valorB

If total < 10 Then
mensagem = "O total é maior que 10"
Else If total > 10 Then
mensagem = "O total é menor que 10"
Else
mesagem = "O total é igual a 10"
End If

TextWindow.WriteLine("Total calculado: " + valorB)
TextWindow.WriteLine(mensagem)
TextWindow.WriteLine("Pressione ENTER para sair...")
TextWindow.Read()

"Problemas para resolver:
    1. Encontre os erros de digitação e os corrija
    2. Entenda a lógica proposta pelo programa 
    3. Corrija os erros de identação
    4. Corrija os erros de declarações das funções
"
